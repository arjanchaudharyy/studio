# Automatically add Signed-off-by line if not already present
if ! grep -q "^Signed-off-by:" "$1"; then
  git interpret-trailers --in-place --trailer "Signed-off-by: $(git config user.name) <$(git config user.email)>" "$1"
fi
