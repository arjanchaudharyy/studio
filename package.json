{
  "name": "shipsec-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.1.20",
  "workspaces": [
    "frontend",
    "backend",
    "worker",
    "packages/backend-client",
    "packages/component-sdk"
  ],
  "scripts": {
    "dev:frontend": "bun --cwd frontend dev",
    "dev:backend": "bun --cwd backend run dev",
    "dev": "bun run dev:frontend",
    "typecheck": "(cd packages/component-sdk && bun run typecheck) && (cd worker && bun run typecheck) && (cd backend && bun run typecheck)",
    "test": "(cd packages/component-sdk && bun test) && (cd worker && bun test) && (cd backend && bun test)"
  },
  "devDependencies": {
    "openapi-typescript": "^7.9.1",
    "pm2": "^6.0.13",
    "tsx": "^4.20.6"
  }
}
